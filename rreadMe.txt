Class: BluetoothManager

Core Methods:
- turnOn()
- turnOff()
- listAvailable()
- pair(device)
- unpair(device)
- isPaired(device)    ✅ (NEW!)
- connect(device)
- disconnect(device)
- isConnected(device)
- getStatus()

Optional Methods:
- trust(device)
- getDeviceInfo(device)
- reset()
